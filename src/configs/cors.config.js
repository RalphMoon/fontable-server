const whitelist = [process.env.CLIENT_URI, process.env.CLIENT_LOCAL_URI];

exports.corsOptions = {
  origin: (origin, callback) => {
    if (whitelist.indexOf(origin) !== -1) {
      callback(null, true);
    } else {
      callback(new Error("Not allowed by CORS"));
    }
  },
  credentials: true,
};
